function dydt = myODE(t, y, params)
    a1 = params[0];
    a2 = params[1];
    b1 = params[2];
    b2 = params[3];
    d1 = params[4];
    d2 = params[5];
    n = params[6];

    dydt = zeros(2,1);
    dydt(1) = a1 /(1+y(2)^n)-b1 * y(1) + d1;
    dydt(2) = a2 / (1+y(1)^n) - b2 * y(2) + d2;
end
